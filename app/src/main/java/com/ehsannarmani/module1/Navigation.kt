package com.ehsannarmani.module1

enum class Navigation (val route:String){
    Home(route = "home"),
    Record(route = "record"),
    Checkpoint(route = "checkpoint"),
    Camera(route = "take-camera")
}